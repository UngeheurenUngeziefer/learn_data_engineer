// print count while less than 11
Integer count = 1;
do {
    System.debug(count);
    count++;
} while (count < 11);

// more intuitive way - same result
Integer count = 1;
while (count < 11) {
    System.debug(count);
    count++;
}
